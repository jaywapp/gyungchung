# 프로젝트 작업 규칙

## 기본 완료 플로우

별도 지시가 없는 변경 작업은 다음 단계를 모두 완료해야 한다.

1. 관련 검사와 프로덕션 빌드를 실행한다.
2. `main`에 직접 커밋하지 않고 작업 브랜치에서 conventional commit을 만든다.
3. 작업 브랜치를 원격 저장소에 푸시한다.
4. `main` 대상 일반 PR을 생성한다.
5. 필수 검사 통과를 확인한 후 PR을 병합한다.
6. 병합된 `main` 기준 운영 배포가 완료됐는지 확인한다.
7. 운영 URL 응답과 최근 오류 로그를 확인한 뒤 결과를 보고한다.

질문, 설명, 코드 리뷰처럼 파일을 변경하지 않는 작업에는 이 플로우를 적용하지 않는다.
사용자가 커밋, 푸시, PR, 병합 또는 배포를 생략하라고 명시하면 해당 지시를 우선한다.
검사 실패, 병합 충돌, 권한 부족 또는 운영 위험이 확인되면 강행하지 말고 원인과 중단 지점을 보고한다.

## Git 및 배포

- 커밋 메시지는 영어 conventional commits 형식을 사용한다.
- 요청 범위 파일만 명시적으로 스테이징한다.
- PR은 검증이 끝난 변경에 대해 일반 PR로 생성하고, 검사 통과 후 병합한다.
- Vercel Git 연동의 `main` 자동 배포를 기본 운영 배포 경로로 사용한다.
- DB 마이그레이션이 포함된 경우 앱 배포 전에 적용 및 검증하고, 마이그레이션 이력을 확인한다.
- 시크릿, 토큰, `.env` 파일은 커밋하거나 로그에 노출하지 않는다.
